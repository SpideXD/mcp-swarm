# MCP Swarm — Dashboard UI Environment Variables
# Copy this file to .env.local and adjust as needed

# Backend URL (where the MCP Swarm backend is running)
# This is also configurable from the dashboard connection input
NEXT_PUBLIC_SWARM_URL=http://localhost:3100
