# MCP Swarm — Backend Environment Variables
# Copy this file to .env and adjust as needed

# Server mode (setting SWARM_PORT enables HTTP mode)
SWARM_PORT=3100
SWARM_HOST=127.0.0.1

# Enable CORS (required for dashboard UI to connect)
SWARM_CORS=1

# Data directory for SQLite database
# SWARM_DATA_DIR=~/.mcp-swarm

# Pool scaling
# MAX_SERVER_INSTANCES=4
# SCALE_UP_WAIT_MS=5000
# IDLE_KILL_MS=60000

# Session limits
# MAX_SESSIONS=50

# Timeouts
# TOOL_CALL_TIMEOUT_MS=60000

# Health watchdog (set to 0 to disable)
# HEALTH_CHECK_INTERVAL_MS=60000
# HEALTH_CHECK_TIMEOUT_MS=10000
